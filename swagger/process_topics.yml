tags:
  - Topics
parameters:
  - name: body
    in: body
    required: true
    schema:
      type: object
      properties:
        topicList:
          type: array
          items:
            type: string
responses:
  200:
    description: A JSON with processed topic data
  500:
    description: Error processing topics
