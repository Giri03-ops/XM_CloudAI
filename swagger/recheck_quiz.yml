tags:
  - Quiz
parameters:
  - name: body
    in: body
    required: true
    schema:
      type: object
      properties:
        existingQuiz:
          type: object
        userAnswers:
          type: object
responses:
  200:
    description: A JSON object containing rechecked quiz data
  500:
    description: Error rechecking quiz
